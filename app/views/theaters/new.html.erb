<% content_for :title, "New Theater | Backoffice" %>

<div class="max-w-lg">
  <div class="flex items-center gap-3 mb-6">
    <%= link_to "← Theaters", theaters_path, class: "text-sm text-gray-500 dark:text-gray-400 hover:text-gray-900 dark:hover:text-gray-100" %>
  </div>

  <h1 class="text-2xl font-bold text-gray-900 dark:text-gray-100 mb-6">New Theater</h1>

  <%= render 'form', theater: @theater %>
</div>
